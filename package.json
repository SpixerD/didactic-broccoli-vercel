{
  "name": "pdf-extractor-license-server-vercel",
  "version": "1.0.0",
  "description": "License validation server for Vercel",
  "main": "api/health.js",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'No build step needed'",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "pg": "^8.11.3",
    "uuid": "^9.0.0"
  },
  "engines": {
    "node": "22.x"
  }
}
